{
  "name": "api-docs",
  "version": "1.0.0",
  "description": "Hosted API docs",
  "main": "src/index.html",
  "author": "Tom Shawver <tom@360mediadirect.com>",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && openapi-merge-cli && echo \"Compiling with Webpack...\" && webpack build",
    "postbuild": "mkdir -p dist/v${npm_package_version} && cp dist/index.html dist/main.js dist/main.js.LICENSE.txt dist/openapi-full.json dist/v${npm_package_version}",
    "merge-specs": "openapi-merge-cli",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "swagger-ui": "^5.28.0"
  },
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "eslint --fix"
    ]
  }
}
